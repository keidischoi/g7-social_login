# Composer
#/vendor/
# composer.lock 은 커밋한다 — 설치 시 vendor 를 잠긴 버전 그대로 재현하기 위해서다.
# (g7 의 plugin:update 는 lock 이 없으면 매번 새로 해석한다)

# OS / editor
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Local / scratch
*.bak
*.bak-*
*.local
*.log

# Local project record (Dev-Project management)
/PROJECT.md
